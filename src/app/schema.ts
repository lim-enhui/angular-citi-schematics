export interface MenuOptions {
    name: string;
    project?: string;
    path?: string;

    module?: string;
    routing_module?: string;
    export?: boolean;
    flat?: boolean;
    routing?: boolean;
}